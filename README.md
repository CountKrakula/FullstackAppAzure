Subscription Manager — A full-stack web application built with ASP.NET Core Web API (backend) and React (frontend), using Entity Framework Core with a SQL database. Users can register, log in, and track their digital subscriptions, costs, categories, and billing intervals, with a dashboard summarizing total monthly cost and cost per category. Deployed to Azure with a CI/CD pipeline via GitHub Actions.
